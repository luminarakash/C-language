// write a function to count the occurrence of vowels in a string.

#include<stdio.h>
#include<math.h>
int main()
{
    int n;
    printf("enter number");
    scanf("%d",&n);
    printf("%f", pow(n, 2));
    return 0;
}